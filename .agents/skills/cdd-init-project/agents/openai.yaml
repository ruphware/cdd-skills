interface:
  display_name: "CDD 01 Init Project"
  short_description: "Empty dir → template; docs-seeded folder → bootstrap + Step 00; boilerplate → Step 00; existing repo → adopt CDD"
policy:
  allow_implicit_invocation: false
