interface:
  display_name: "[CDD-0] Boot"
  short_description: "CDD repo boot with docs, threads, worktree routing"
  default_prompt: "Use $cdd-boot to ingest AGENTS.md, read canonical project/development docs plus any referenced external thread or artifact with comments, prefer the main checkout for read-only research/audit intents, and recommend the right cdd-* continuation."
policy:
  allow_implicit_invocation: false
