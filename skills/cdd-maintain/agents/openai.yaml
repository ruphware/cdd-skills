interface:
  display_name: "CDD 07 Maintain"
  short_description: "Codebase maintenance: Archive CDD files, Doctor the codebase"
  default_prompt: "Use $cdd-maintain to archive long TODO and journal files, check docs/INDEX.md freshness, and doctor the codebase for refactor severity plus dead or orphaned code."
policy:
  allow_implicit_invocation: false
